# 20260813 核心设计追踪矩阵

唯一设计基准为 `基于目标器官的采样方法-20260813.docx` 的接受修订视图，SHA-256 为 `de56e7a1b984f925e97631b076d6b729e77575eb6513b4d57f3028818b7e71ca`。项目说明、README 和历史设计文件只作为实现或历史证据，不可覆盖核心设计。

| ID | 核心段落 | 当前验收要求 | 实现与自动化证据 | 状态 |
|---|---|---|---|---|
| R01 | P001-P009 | 患者坐标采用 RAS，解剖平面与轴向一致 | `coordinates.py`；坐标、CT 和网格测试 | 符合 |
| R02 | P010-P015 | 局部 x/y/z 为内镜前进、左右、上下轴；探头位于方形底边中点；三轴分别绕 x/y/z | `squares.py`；右手系、底边中心、非零旋转测试 | 符合 |
| R03 | P016-P019 | 胃、食管、十二指肠外法线命中目标器官；食管原有效段和完整下移段均执行同一有效点规则 | `sampling_pipeline.py`；真实位置双重射线调用和命中证据测试 | 符合 |
| R04 | P021-P023 | 胰腺空间范围与法线角严格按 RAS 不等式筛选 | `sampling.py`；极值平台及严格角度边界测试 | 符合 |
| R05 | P024-P025 | 肝一区使用 `x_min+20 mm`、对应 y 范围和向下法线 | `sampling.py`；20 mm、闭区间和法线测试 | 符合 |
| R06 | P027-P028 | 肝二区使用脾/下腔静脉 x 范围、胰腺前缘和 Y/Z 法线角 | `sampling.py`；边界和来源组合测试 | 符合 |
| R07 | P030 | 基础区和补充区出现相同位姿时保留基础区 | `sampling_pipeline.py`；精确位姿去重与优先级测试 | 符合；重叠按用户确认的同一位姿判定 |
| R08 | P031-P036 | 十二指肠球部范围和中心线 0 度面；逆 30°、顺 120°、5° 步长 | `centerline.py`、`squares.py`；球部边界、0 度面和 7657 姿态测试 | 符合 |
| R09 | P037-P040 | 十二指肠余部范围、排除球部；双向 30°、5° 步长 | `sampling.py`、`squares.py`；区域互斥和 3211 姿态测试 | 符合 |
| R10 | P042-P043 | 普通区由食管下缘参考线与外/内法线构造 0 度面 | `squares.py`；普通与补充区轴向、退化候选测试 | 符合 |
| R11 | P044-P045 | 胰腺特殊区位于主动脉最大 x 右侧；逆 30°、顺 120° | `sampling_pipeline.py`、`squares.py`；分类和 7657 姿态测试 | 符合 |
| R12 | P047-P048 | 其余部分偏航双向 30°、5° 步长 | `squares.py`；角集合测试 | 符合 |
| R13 | P049-P051 | 俯仰双向 30°、滚动双向 45°，均为 5° 步长 | `contract.py`、`squares.py`；完整角集合、笛卡尔积和运行元数据测试 | 符合 |

## 已确认的实现约定

- 顺逆时针从局部 `+z` 侧朝探头观察：正 yaw 为逆时针、负 yaw 为顺时针；球部和胰腺特殊区均编码为 `-120..+30/5 deg`。
- 三轴采用局部内禀 `B @ Rz(yaw) @ Ry(pitch) @ Rx(roll)`；所有旋转围绕探头点，探头始终是方形底边中点。
- 基础区与补充区的“重叠”按四顶点在 `rtol=0, atol=1e-9 mm` 下相同的位姿判定，不按邻近点或一般视野相交删除。
- 食管先构造原有效段和完整下移段，再分别在各自 RAS 坐标沿外法线做目标射线筛选；不得复制旧位置命中标签。

## DOCX 未规定但经用户确认保留

- 同一区域采样点 RAS 欧氏间距至少 10 mm；配置点数为上限。
- 外法线目标器官射线最长 100 mm，按 2048 条分块。
- 100 mm 方形、300 x 300 输出、CT 三次 B-spline、WL 40、WW 400、FOV 外 -1000 HU。
- 手工标签最近邻采样、三类 EUS 血管独立结果和原 PLY 血管链路不变；手工病例黑色比例严格大于 60% 才拒绝。
- 病例 2 十二指肠主路径为 RAS E1 `[19,24,700]` 到 E2 `[-33,1,664]`，匹配容差 1 mm。

## 验证边界

- 论文发布整理后的本地完整套件为 `221 passed`；3 条警告均来自 SimpleITK/SWIG 运行时弃用提示。
- 自动化测试覆盖患者坐标转换、物理空间插值、器官筛选、10 mm FPS、十二指肠中心线、三轴姿态、手工标签、三类 EUS 血管、质量/FOV、恢复协议、内部索引和输出审计。
- 病例采样点数、各偏航策略点数和最终姿态总数必须由对应输入几何的 dry-run 或正式 `run_metadata.json` 给出，不得从历史病例常量推断。
- SimpleITK/SWIG 运行时弃用警告不改变像素或几何结果，但环境升级时仍需重新执行 CPU/GPU 对照与完整测试。

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ct-vascular-resampling"
version = "0.1.0"
description = "无 P/N/D 输入的 CT 与血管模型重采样图库"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
